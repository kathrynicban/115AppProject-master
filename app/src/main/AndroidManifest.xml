<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.aquio.icban">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".titleActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".homeActivity" />
        <activity android:name=".Aerobics" />
        <activity android:name=".Strength" />
        <activity android:name=".flexibilityActivity" />
        <activity android:name=".balanceActivity" />
        <activity android:name=".videoPreview" />
        <activity android:name=".seriesActivity"></activity>
    </application>

</manifest>